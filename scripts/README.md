# Scripts
This directory contains utility scripts for the fintech review analytics pipeline.

- `scrape_reviews.py`: Live scraping from Google Play Store.
- `task_2_analysis.py`: Performs sentiment and thematic analysis on raw data.

- # Scripts
This directory contains utility scripts for the fintech review analytics pipeline.

- `db_insert.py`: Loads cleaned review CSV data into PostgreSQL using `schema.sql`.

Use this script when you want to persist processed review data into the `banks` and `reviews` tables for reporting and analytics.
